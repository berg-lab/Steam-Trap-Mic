#include "flash.h"

void CFlash::deviceSetup() {
//   if (flash.initialize()) {
//     uint8_t *mac_address = flash.readUniqueId();
//     memcpy(UID, mac_address, 8*sizeof(uint8_t));
//     Serial.println(UID);
//     error_code &= ~(1<<FLASH_ERR);
//   }
//   else {
//     Serial.println("Flash initialization failed!");
//     error_code |= (1<<FLASH_ERR);
}